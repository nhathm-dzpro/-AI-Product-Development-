# Copy to .env (local only — NEVER commit .env)
PORT=5000
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/alnrestaurant?schema=public"
JWT_SECRET="change-this-secret"
JWT_EXPIRES_IN="7d"
AI_API_KEY=""
